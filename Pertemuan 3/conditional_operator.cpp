#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    int nilai;
    // (statement) ? true : false;
    char lulus = nilai > 70 ? 'Y' : 'N';
    cout << lulus << endl;
}
